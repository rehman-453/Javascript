/*
Q12. Write a function that calculates the area of a rectangle. A = width * height Pass 
    width and height in following manner:
    a. Arguments as values
    b. Arguments as variables
*/

document.write("Area of triangle = "+ Area_of_triangle(2,2) +' unit<sup>2</sup>')

function Area_of_triangle(width,height)
{
    Area = width * height
    return(Area)
}
